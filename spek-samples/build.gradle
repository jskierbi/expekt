apply plugin: 'application'

mainClassName = 'org.jetbrains.spek.console.ConsoleRunnerKt'
run { args ".", "org.jetbrains.spek.samples", "-f", "text" }

description = 'Spek Samples'

dependencies {
    compile project(':spek-core')
}
